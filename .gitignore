# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# ---- Virtual environments ----
.venv/
venv/
env/

# ---- Secrets / local env (API keys must NEVER be committed) ----
.env
.env.*
*.key
secrets.*

# ---- IDE ----
.idea/
.vscode/

# ---- Claude Code local settings (machine-specific permissions) ----
.claude/settings.local.json
.claude/launch.json

# ---- Runtime state (regenerated on first boot / run) ----
# The web app pre-trains and persists these on first boot by design;
# a fresh clone should regenerate them, not inherit a stale copy.
*.db
bandit_state.json

# ---- Ahrefs raw exports (large; the committed data/ahrefs/*.json is derived) ----
# Drop weekly exports here and run import_ahrefs.py; only the small JSON is committed.
data/ahrefs/raw/
data/ahrefs/*.csv

# ---- OS ----
.DS_Store
Thumbs.db
