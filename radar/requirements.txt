fastapi>=0.110
uvicorn>=0.29
psycopg[binary]>=3.1
# psycopg is the only non-stdlib dependency beyond the web framework, and it's
# only used at all when DATABASE_URL is set (radar/store.py) — for the daily
# history archive. Everything else (Apify + Claude calls, JSON, scoring) is
# the Python standard library, so this folder still ports cleanly.
